---
name: Release

# Trigger the workflow on manual dispatch and when merging a PR.
# If literalizer-cli's version has not changed, the tagging will fail.
# If literalizer-cli's version has changed, the tagging will succeed and a new release will
# be created.
# This means that the version can stay in sync, and if the hook needs to be updated
# to a new version to release changes in this repository, a new release of literalizer-cli is needed.
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Publish a release
    runs-on: ubuntu-latest
    environment: release

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6
        with:
          # See
          # https://github.com/stefanzweifel/git-auto-commit-action?tab=readme-ov-file#push-to-protected-branches
          token: ${{ secrets.RELEASE_PAT }}
          # Fetch all history including tags.
          # Needed to find the latest tag.
          #
          # Also, avoids
          # https://github.com/stefanzweifel/git-auto-commit-action/issues/99.
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: '**/pyproject.toml'

      # We have a race condition.
      # In particular, we push literalizer-cli to PyPI and then not long after we try to install
      # the pushed version.
      # Here, we give PyPI time to propagate the package.
      - name: Install literalizer-cli from PyPI
        uses: nick-fields/retry@v4
        with:
          timeout_seconds: 20
          max_attempts: 20
          command: uv sync --refresh

      - name: Get literalizer-cli version
        id: get_version
        run: |
          output=$(uv run literalize --version)
          version="${output##* }"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Bump version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          custom_tag: ${{ steps.get_version.outputs.version }}
          tag_prefix: v

      - name: Create a GitHub release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.tag_version.outputs.new_tag }}
          makeLatest: true
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: ${{ steps.tag_version.outputs.changelog }}

      # Use regex to find any version pattern instead of looking for a specific version.
      # This is more reliable because it works even if the README is out of date
      # or has been manually edited - it will always find and update the version
      # regardless of what the old version was.
      - name: Update README versions
        id: update_readme_versions
        uses: jacobtomlinson/gha-find-replace@v3
        with:
          find: '(rev: )v[0-9]+\.[0-9]{1,2}\.[0-9]{1,2}(\.[0-9]+)?'
          replace: ${1}${{ steps.tag_version.outputs.new_tag }}
          include: README.rst
          regex: true

      - name: Check Update README versions was modified
        run: |
          if [ "${{ steps.update_readme_versions.outputs.modifiedFiles }}" = "0" ]; then
            echo "Error: No files were modified when updating README versions"
            exit 1
          fi
      - uses: stefanzweifel/git-auto-commit-action@v7
        id: commit-readme
        with:
          commit_message: Replace version in README
          branch: main
          file_pattern: README.rst
          # Error if there are no changes.
          skip_dirty_check: true
